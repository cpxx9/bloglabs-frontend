import PostsList from '../../components/Posts/Posts/PostsList';
import StyledRoot from './StyledRoot';

const Root = () => {
  return (
    <StyledRoot>
      <PostsList />
    </StyledRoot>
  );
};

export default Root;
